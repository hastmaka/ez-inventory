# Commands

```bash
npm run dev:stripe_test   # Dev server (Stripe test keys)
npm run dev:stripe_live   # Dev server (Stripe live keys)
npm run bd:stripe_test    # Build + Firebase deploy (Stripe test)
npm run bd:stripe_live    # Build + Firebase deploy (Stripe live)
npm run lint              # ESLint check
```
