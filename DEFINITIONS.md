### ***Definitions***
________
1 - Assets

assets_name + _name

client_sex_name

Info: Always sending the label corresponding to a value to use  in the ***dropdown*** directly.

2 - Models

wherever_full_name

example: client_full_name


3 - CONCAT

wherever_wherever_concat

example: there are some cases where you need as a descriptive way add something to the string, ex: in caregiver name we add relation with the client (Mother)




### VIEW RULES
___
All data needed in the UI where is only to see, it's going to be concat server side, sending only the string to render